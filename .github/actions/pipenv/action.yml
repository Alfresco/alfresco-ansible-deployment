name: install_python_packages
description: 'Install python packages and create pipenv'

runs:
  using: "composite"
  steps: 
    steps:
    - name: Install pipenv and Pipfile
      shell: bash
      run: |
        pip install --user pipenv
        pipenv install