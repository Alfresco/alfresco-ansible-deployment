name: 'Python packages'
description: 'Install and cache python packages and set up virtual pipenv'
jobs:
  install_python_packages:
    steps:
      - name: Install pipenv and its packages in virtual environment
      run: pip install --user pipenv
      run: pipenv install
      