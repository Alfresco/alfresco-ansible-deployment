all:
  children:
    external:
      children:
        external_activemq:
          hosts:
            external_activemq_hostname:
              activemq_username: alfresco
              activemq_port: 61617
              activemq_protocol: ssl
