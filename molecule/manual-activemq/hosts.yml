all:
  children:
    activemq:
      hosts:
        activemq-instance:
          activemq_username: alfresco
          activemq_password: HbDUV2JwzfUFf6ab # pragma: allowlist secret
    external:
      children:
        external_activemq:
          hosts:
            external_activemq_hostname:
              activemq_username: alfresco
              activemq_password: HbDUV2JwzfUFf6ab # pragma: allowlist secret
              activemq_port: 61616
