all:
  children:
    external:
      children:
        external_activemq:
          hosts:
            localhost:
              activemq_username: admin
              activemq_password: admin
              activemq_port: 61616
