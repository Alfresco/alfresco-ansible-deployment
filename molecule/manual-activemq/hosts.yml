all:
  children:
    external:
      children:
        external_activemq:
          hosts:
            external_activemq_hostname:
              activemq_username: admin
              activemq_password: admin
              activemq_port: 61616
