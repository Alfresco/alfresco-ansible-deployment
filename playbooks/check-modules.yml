- hosts: localhost
  tasks:
    - name: Check pip module versions
      shell: |
        ../scripts/compare-modules.sh
        echo $?
      register: echo
    - name: Exit playbook if modules do not match
      fail:
        msg: "{{ echo['stdout_lines'][0] }}"
      when: echo['stdout_lines'][0] != "0"
      