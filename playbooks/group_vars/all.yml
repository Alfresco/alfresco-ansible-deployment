default_java_version: 17.0.14+7
acs_play_java_core: "{{ default_java_version.split('+')[0] }}"
ats_mtls_capable: >
  {{
    acs.version is version('7.4', 'ge') and
    transform.version is version('3.1', 'ge') and
    trouter.version is version('2.1', 'ge') and
    acs_play_sfs_version is version('2.1', 'ge')
  }}
