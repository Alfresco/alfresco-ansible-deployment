---
all:
  children:
    database:
      hosts:
        database_1:         
          ansible_connection: local
          ### For connecting to external machines you will need the host,private key file and user. You can also add ssh common args but are optional.
          # ansible_host: targetIP
          # ansible_private_key_file: "/path/ssh_key.pem"
          # ansible_user: centos           
          # #ansible_ssh_common_args: "-o UserKnownHostsFile=/dev/null -o ControlMaster=auto -o ControlPersist=60s -o ForwardX11=no -o LogLevel=ERROR -o IdentitiesOnly=yes -o StrictHostKeyChecking=no"
    repository:
      hosts:
        repository_1:
          ansible_connection: local
    activemq:
      hosts:
        activemq_1:
          ansible_connection: local
    search:
      hosts:
        search_1:
          ansible_connection: local
    webservers:
      hosts:
        nginx_1:
          ansible_connection: local
    adw:
      hosts:
        adw_1:
          ansible_connection: local
    transformers:
      hosts:
        transformers_1:
          ansible_connection: local
    syncservice:
      hosts:
        syncservice_1:
          ansible_connection: local