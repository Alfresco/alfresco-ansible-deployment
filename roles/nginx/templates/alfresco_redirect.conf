server {
    access_log {{ logs_folder }}/nginx.alfresco.access.log;
    error_log  {{ logs_folder }}/nginx.alfresco.error.log error;

    listen {{ ports_cfg.nginx.http | default('80') }};

{% if item.server_name is defined %}
    server_name {{ item.server_name }};
{% endif %}

   map $remote_addr $unknown_repo_client {
      default 1;
{% for trusted_client in [ share_host, solr_host, sync_host ] %}
      {{ trusted_client }} 0;
{% endfor %}
   }
   if ($unknown_repo_client) {
      return 301 https://$host$request_uri;
   }
}
