server {

    access_log  /var/log/alfresco/nginx.alfresco.access.log;
    error_log /var/log/alfresco/nginx.alfresco.error.log error;

        listen {{ item.listen | default('80') }}{% if item.listen == '443' %} ssl{% endif %};

	{% if item.server_name is defined %}
	    server_name {{ item.server_name }};
	{% endif %}

        {% if item.listen == '443' %}
	ssl_certificate /etc/nginx/{{ item.cert_crt }};
	ssl_certificate_key /etc/nginx/{{ item.cert_key }};
        {% endif %}

        client_max_body_size 0;

        set  $allowOriginSite *;
        proxy_pass_request_headers on;
        proxy_pass_header Set-Cookie;

        location / {
            {% if item.root is defined %}
              root {{ item.root }};
            {% endif %}
            index {{ item.index | default('index.html index.htm') }};
        }

        # External settings, do not remove
        #ENV_ACCESS_LOG

        location /aio/ {
            proxy_pass http://{{ ats_tengine_aio_host }}:8090/;
            proxy_redirect off;
            proxy_buffering off;
            proxy_set_header Host            $host:$server_port;
            proxy_set_header X-Real-IP       $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_pass_header Set-Cookie;
        }

        location /digital-workspace/ {
            proxy_pass http://{{ adw_host }}:8880/;
            proxy_redirect off;
            proxy_buffering off;
            proxy_set_header Host            $host:$server_port;
            proxy_set_header X-Real-IP       $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_pass_header Set-Cookie;
        }

        location /share/ {
            proxy_pass http://{{ share_host }}:8082;
            proxy_redirect off;
            proxy_buffering off;
            proxy_set_header Host            $host:$server_port;
            proxy_set_header X-Real-IP       $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_pass_header Set-Cookie;
        }

        location /alfresco/ {
            proxy_pass http://{{ repo_host }}:8080;
            proxy_redirect off;
            proxy_buffering off;
            proxy_set_header Host            $host:$server_port;
            proxy_set_header X-Real-IP       $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_pass_header Set-Cookie;
        }

        location /jolokia/ {
            proxy_pass http://{{ repo_host }}:8080;
            proxy_redirect off;
            proxy_buffering off;
            proxy_set_header Host            $host:$server_port;
            proxy_set_header X-Real-IP       $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_pass_header Set-Cookie;
        }
        location /api-explorer/ {
            proxy_pass http://{{ repo_host }}:8080;
            proxy_redirect off;
            proxy_buffering off;
            proxy_set_header Host            $host:$server_port;
            proxy_set_header X-Real-IP       $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_pass_header Set-Cookie;
        }
        
        location /solr/ {
            proxy_pass http://{{ solr_host }}:8983;
            proxy_redirect off;
            proxy_buffering off;
            proxy_set_header Host            $host:$server_port;
            proxy_set_header X-Real-IP       $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_pass_header Set-Cookie;
        }

    }
