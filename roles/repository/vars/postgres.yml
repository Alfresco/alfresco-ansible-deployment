---
postgres_client_config:
  - database: "alfresco"
    user: "alfresco"
    passwd: "alfresco"
    port: "5432"
    loginhost: "{{ hostvars['postgres1']['ansible_host'] }}"
    loginpassword: "{{ postgres_pass }}"    