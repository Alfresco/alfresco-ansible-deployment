---
- name: tomcat-start
  systemd:
    name: "alfresco-content"
    state: started
    daemon_reload: yes

- name: tomcat-stop
  systemd:
    name: "alfresco-content"
    state: stopped
    daemon_reload: yes