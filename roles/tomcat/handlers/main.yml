---
- name: tomcat-start
  systemd:
    name: "alfresco-content"
    state: started
    daemon_reload: true

- name: tomcat-stop
  systemd:
    name: "alfresco-content"
    state: stopped
    daemon_reload: true