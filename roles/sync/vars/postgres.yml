---
postgres_client_config:
  - database: "alfresco-sync"
    user: "alfresco"
    passwd: "alfresco"
    port: "5432"
postgres_pass: MyPass1
db_host_connect: "{{ hostvars['database_1']['ansible_host'] }}"
sync_connect: "{{ hostvars['syncservice_1']['ansible_host'] }}"