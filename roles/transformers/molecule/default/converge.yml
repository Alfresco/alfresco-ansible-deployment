---
- name: Converge
  hosts: all
  tasks:
    - name: "Include activemq"
      include_role:
        name: "activemq"
    - name: "Include transformers"
      include_role:
        name: "transformers"
