---
- name: Converge
  hosts: all
  tasks:
    - name: "Include tomcat role"
      include_role:
        name: "tomcat"
    - name: "Include transformers"
      include_role:
        name: "transformers"
