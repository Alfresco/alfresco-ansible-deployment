    - name: Fail if external and local activemq is in use
      fail:
        msg: Detected both external_activemq and activemq groups in use
      when:
        - groups.external_activemq is not empty
        - groups.activemq is not empty
      tags:
        - molecule-notest
