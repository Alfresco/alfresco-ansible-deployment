---
# vars file for common
#nginx_domain: ""

nginx_vhost_path: /etc/nginx/conf.d
nexus_user: "{{ lookup('env', 'NEXUS_USERNAME') }}"
nexus_password: "{{ lookup('env', 'NEXUS_PASSWORD') }}"

group_name: alfresco
username: alfresco

maven_repository:
   org: "https://repo.maven.apache.org/maven2/org"
licenses: 3.1.0

repo_host: "{{ hostvars['repository_1']['ansible_host'] | default(localhost) }}"
activemq_host: "{{ hostvars['activemq_1']['ansible_host'] | default(localhost) }}"
db_host: "{{ hostvars['database_1']['ansible_host'] | default(localhost) }}"
solr_host: "{{ hostvars['search_1']['ansible_host'] | default(localhost) }}"
share_host: "{{ repo_host }}"
ats_tengine_aio_host: "{{ hostvars['transformers_1']['ansible_host'] | default(localhost) }}"
sfs_host: "{{ hostvars['transformers_1']['ansible_host'] | default(localhost) }}"
trouter_host: "{{ hostvars['transformers_1']['ansible_host'] | default(localhost) }}"
adw_host: "{{ hostvars['adw_1']['ansible_host'] | default(localhost) }}"
sync_host: "{{ hostvars['syncservice_1']['ansible_host'] | default(localhost) }}"