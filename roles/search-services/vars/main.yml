---

utils:
  - unzip
  - lsof 
  - ca-certificates
  - wget
  - openssl
  - openssl-devel

dist_dir: "{{ binaries_folder }}/search-services-{{ solr.version }}"

config_dir: "{{ config_folder }}/search-services"

data_dir: "{{ data_folder }}/search-services"
