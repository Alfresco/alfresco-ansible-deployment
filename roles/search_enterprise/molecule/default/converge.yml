---
- name: Converge
  hosts: all
  tasks:
    - name: "Include activemq"
      ansible.builtin.include_role:
        name: "activemq"
    - name: "Include sfs"
      ansible.builtin.include_role:
        name: "sfs"
      vars:
        sfs_zip_username: "{{ lookup('env', 'NEXUS_USERNAME') }}"
        sfs_zip_password: "{{ lookup('env', 'NEXUS_PASSWORD') }}"
    - name: "Include transformers"
      ansible.builtin.include_role:
        name: "transformers"
    - name: "Include t-router"
      ansible.builtin.include_role:
        name: "trouter"
    - name: "Include search_enterprise"
      ansible.builtin.include_role:
        name: "search_enterprise"
      vars:
        search_enterprise_zip_username: "{{ lookup('env', 'NEXUS_USERNAME') }}"
        search_enterprise_zip_password: "{{ lookup('env', 'NEXUS_PASSWORD') }}"
