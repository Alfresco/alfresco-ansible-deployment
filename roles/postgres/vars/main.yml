---
external_ip: "{{ hostvars['repository1']['ansible_host'] }}"
postgres_client_auth:
  - type: local
    database: "alfresco"
    user: "alfresco"
    address:
    method: password
  - type: host
    database: all
    user: all
    address: 127.0.0.1/32
    method: md5
  - type: host
    database: all
    user: all
    address: "{{ external_ip }}/32"
    method: md5
