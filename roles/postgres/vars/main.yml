---
postgres_pass: MyPass1
postgres_client_auth:
  - type: local
    database: "alfresco"
    user: "alfresco"
    address:
    method: password
  - type: local
    database: "alfresco_sync"
    user: "alfresco"
    address:
    method: password
  - type: host
    database: all
    user: all
    address: 127.0.0.1/32
    method: md5
  - type: host
    database: all
    user: all
    address: "{{ hostvars['repository_1']['ansible_host'] }}/32"
    method: md5
  - type: host
    database: all
    user: all
    address: "{{ hostvars['syncservice_1']['ansible_host'] }}/32"
    method: md5
