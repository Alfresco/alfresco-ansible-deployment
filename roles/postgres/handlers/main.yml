---
- name: restart-postgresql
  become: yes
  service:
    name: "{{ postgresql_service }}"
    state: restarted

- name: enable-postgresql
  become: yes
  service:
    name: "{{ postgresql_service }}"
    enabled: true
    daemon_reload: true
