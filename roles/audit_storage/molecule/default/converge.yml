---
- name: Converge
  hosts: all
  roles:
    - role: activemq
      vars:
        activemq_version: "{{ dependencies_version.activemq }}"
    - role: elasticsearch
      vars:
        elasticsearch_version: "{{ dependencies_version.elasticsearch_major_version }}"
    - role: audit_storage
