---
- name: Converge
  hosts: all
  vars:
    repository_acs_version: 23.4.1
  roles:
    - role: activemq
    - role: elasticsearch
      vars:
        elasticsearch_major_version: "{{ '8.x' if repository_acs_version is version('25.0', 'ge') else '7.x' }}"
    - role: audit_storage
